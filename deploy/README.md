# Deployment

This folder contains deployment-related artifacts (Docker, compose, k8s, etc.).
The provided `docker-compose.yml` is only a starting point and should be adapted
to your actual environment (paths, volumes, images, etc.).
